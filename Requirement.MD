# 需求
1. 是一个web页面，技术栈用react，打包成一个单独的可执行的html文件
2. 主要是用于便捷的编写sql
3. 用于db2数据库，在mocha连接AS400环境执行sql，所以需要一个长度限制功能
4. 由于数据库字段很难记忆，需要一个功能导入dds和copybook，关联字段和dds，把关联关系存入软件中
5. 需要有一个搜索框搜索字段

# 详细功能
1. 需要一个功能crud表关系，主要是存入数量关系（一对多多对多多对一），关联关系（通过哪个字段关联）
2. 左侧搜索栏，中部主编辑器，用monaco这个组件作为编辑器。
3. 左侧可以搜索表，搜索字段，搜索到表之后，点击可以自动把表关联进sql（根据配置的关联关系）
4. 搜索字段，点击后自动把字段加入到select里面去

# 对话后总结的需求

以下内容在 UI 原型与讨论中已对齐，后续开发（React + 单文件 HTML 等）可据此实现，减少对聊天上下文的依赖。

## 产品与技术目标

- Web 工具，**技术栈 React**，最终 **打包为单个可执行的 HTML**（或等价单文件分发）。
- 用途：在 **DB2 / AS400** 环境下便捷编写 SQL；通过 **Mocha** 连接执行，需考虑 **SQL 总长度上限**（如 32767）及 **终端行宽** 限制。

## 主界面布局（与 `ui-mockup.html` 一致的方向）

- **左上角**：VS Code 式 **主菜单**（如 **File**、**Settings**）。**Settings** 打开 **设置 Modal**，不在主界面堆叠「关联」等复杂配置。
- **左侧**：**仅保留搜索**（表名 / 字段名可合并为一个搜索框与统一结果列表）。点击表 → 按配置的 **表关系** 自动拼 **FROM/JOIN**；点击字段 → 将字段加入当前 **SELECT**。
- **中部**：**Monaco** 作为主编辑器；格式化 / 换行 / 压缩策略由配置驱动。
- **顶栏**：可保留 SQL 总长度指示、执行等；**表关系 CRUD、DDS 路径等不放在顶栏**，统一进设置。

## 设置 Modal 中的配置项

### 1. DDS / Copybook 路径（多组、有序）

- 可配置 **多组**，每组包含：
  - **DDS 目录**
  - **Copybook 目录**
- **自动配对规则**：同组内按文件名 **basename** 配对，约定 **`表名.dds`** 与 **`表名.cbl`**（copybook 以 `.cbl` 结尾）。
- **有序列表**：组与组之间有 **先后顺序**。若多套路径下出现 **重复表名**，以 **排序靠前** 的组为准（解析/索引时先命中先用）。

### 2. 表关系

- **CRUD**：维护表与表之间的 **数量关系**（一对多、多对多、多对一等）及 **关联条件**（通过哪些字段 / ON 子句）。
- 可配置 **「表关系外部配置路径」**（目录或文件）：实现阶段应能 **扫描/读取并合并** 到统一配置中，供后续查找；避免运行时分散读多个异构路径。

### 3. SQL 行长与压缩（适配 Mocha 窗口）

- **每行最大字符数**：超过则 **自动换行**（实现时在合适断点断开）。
- **尽可能压缩 SQL**（如减少多余空白/换行），便于粘贴到 **行宽受限** 的 Mocha 界面。

### 4. 配置持久化：JSON + 导入导出

- 全部配置以 **JSON** 持久化；设置内提供 **导入 / 导出**（与菜单 **File** 侧一致能力亦可）。
- **运行时**：搜索表/字段、插 SQL、解析 DDS-Copybook 映射等，优先 **只读合并后的配置 JSON**（及内存索引），而不是每次分散读原始路径上的多份文件（除非设计为启动时同步重建索引）。

### 5. 表关系路径 → 配置 JSON

- 支持将 **表关系配置路径** 上的内容 **转为 / 合并进** 同一份 **配置 JSON**（例如 `relations` 与 **`relationIndex`** 等查找结构）；后续 **查找、联想、插 JOIN** 均基于该 JSON（或由其构建的索引）。

## 参考 JSON 字段（实现时可微调命名，但语义建议保留）

- `version`
- `ddsCopybookPathGroups[]`：`order`、`ddsPath`、`copybookPath`、`pairing`（如 `ddsSuffix: ".dds"`, `copybookSuffix: ".cbl"`）
- `tableResolution` 或对外的说明字段：明确 **多组顺序与同名表优先级**
- `relations[]`：表关系列表（含基数、ON 条件等）
- `sqlFormatting`：`maxCharsPerLine`、`wrapLongLines`、`compressSql`
- `tableRelationSourcePath`：外部关系配置路径（可选）
- `relationIndex`：由路径扫描或合并生成的 **查找用索引**（结构由实现定义）

## 原型文件

- 交互与文案以仓库内 **`ui-mockup.html`** 为 UI/流程参考（含菜单、设置 Modal、JSON 预览与演示用导出逻辑）。

## 与文档前半部分的关系

- 上文「# 需求」「# 详细功能」仍为原始条目；**本章节为基于对话的补充与定稿级说明**，若有冲突以 **「对话后总结的需求」** 中与原型、配置 JSON 相关的部分为准，或开发前再合并修订两处标题。